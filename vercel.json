{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/[...path].mjs": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/images/(.*)",
      "destination": "/api/images/$1"
    }
  ]
}
